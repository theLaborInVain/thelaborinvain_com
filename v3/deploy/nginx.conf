server {
    listen 443;
    server_name dev.thelaborinvain.com;

    ### Logging
    access_log  /var/log/nginx/dev_thelaborinvain_com.access.log;
    error_log   /var/log/nginx/dev_thelaborinvain_com.error.log;

    location / {
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Host      $http_host;
        proxy_pass         http://127.0.0.1:9999;

        # Enable Gzip
        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
        gzip_min_length 1000;
        gzip_proxied any;
        gzip_vary on;
    }

    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    }

}

server {
    listen 443;
    server_name thelaborinvain.com;

    ### Logging
    access_log  /var/log/nginx/thelaborinvain_com.access.log;
    error_log   /var/log/nginx/thelaborinvain_com.error.log;

    location / {
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   Host      $http_host;
        proxy_pass         http://127.0.0.1:9090;

        # Enable Gzip
        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
        gzip_min_length 1000;
        gzip_proxied any;
        gzip_vary on;
    }

    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    }

}
